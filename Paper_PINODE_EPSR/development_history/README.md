# Development history

This folder preserves patch-era notes and immutable snapshots of the pre-reorganization regression tests.
The canonical implementation is under `src/pinode_epsr`; development-history material is not part of the runtime package.
