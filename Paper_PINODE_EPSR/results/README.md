# Generated results

Do not place large/generated paper results in the repository.

The default shared location is resolved relative to the main ScaleBridge checkout as:

`../../Data/ScaleBridge/Paper_PINODE_EPSR`

from `NewOrg/scalebridge-research`, with environment/explicit overrides supported by the code.

Small approved validation evidence belongs in `../validation/`.
