diff --git a/scripts/energyplus/smoke_test_office_small_seattle.py b/scripts/energyplus/smoke_test_office_small_seattle.py
index d957e17..752da23 100644
--- a/scripts/energyplus/smoke_test_office_small_seattle.py
+++ b/scripts/energyplus/smoke_test_office_small_seattle.py
@@ -16,6 +16,7 @@ from __future__ import annotations
 
 import json
 from pathlib import Path
+from uuid import uuid4
 
 import opyplus
 
@@ -113,12 +114,14 @@ def main() -> None:
     # Phase 2: Build the case and isolated smoke-test run directories.
     # ----------------------------------------------------------------------
     case_spec = build_smoke_case(data_root)
-    run_root = (
+    case_root = (
         generated_root
         / "smoke_tests"
         / "office_small_seattle"
         / case_spec.case_id
     )
+    run_id = f"eprun_{uuid4().hex[:12]}"
+    run_root = case_root / "runs" / run_id
     input_root = run_root / "inputs"
     raw_root = run_root / "raw"
     input_root.mkdir(parents=True, exist_ok=True)
@@ -156,6 +159,7 @@ def main() -> None:
 
     summary = {
         "case_id": case_spec.case_id,
+        "run_id": run_id,
         "energyplus_root": str(energyplus_root),
         "prepared_idf": str(preparation.prepared_idf_path),
         "simulation_directory": str(run_result.output_directory),
@@ -185,9 +189,15 @@ def main() -> None:
         json.dumps(summary, indent=2, sort_keys=True) + "\n",
         encoding="utf-8",
     )
+    latest_summary_path = case_root / "latest_smoke_test_summary.json"
+    latest_summary_path.write_text(
+        json.dumps(summary, indent=2, sort_keys=True) + "\n",
+        encoding="utf-8",
+    )
 
     print(json.dumps(summary, indent=2, sort_keys=True))
     print(f"Summary: {summary_path}")
+    print(f"Latest summary: {latest_summary_path}")
 
     if not run_result.completed_successfully:
         raise SystemExit(1)
diff --git a/src/scalebridge/integration/energyplus/outputs/eio.py b/src/scalebridge/integration/energyplus/outputs/eio.py
index ce457bc..a8d00c3 100644
--- a/src/scalebridge/integration/energyplus/outputs/eio.py
+++ b/src/scalebridge/integration/energyplus/outputs/eio.py
@@ -69,8 +69,8 @@ def parse_eio(path: str | Path) -> dict[str, EioTable]:
                     rows.setdefault(category, [])
                 continue
 
-            category = _extract_category(first)
-            if category is not None and category in headers:
+            category = _extract_category(first) or first
+            if category in headers:
                 rows[category].append(fields[1:])
 
     return {
diff --git a/tests/integration/energyplus/test_output_extractor.py b/tests/integration/energyplus/test_output_extractor.py
index 1fdc525..5064e11 100644
--- a/tests/integration/energyplus/test_output_extractor.py
+++ b/tests/integration/energyplus/test_output_extractor.py
@@ -174,7 +174,7 @@ def test_parse_eio_preserves_category_columns_and_rows(tmp_path: Path) -> None:
     path = tmp_path / "eplusout.eio"
     path.write_text(
         "! <Zone Information>,Zone Name,Description\n"
-        '<Zone Information>,CORE_ZN,"Core, occupied"\n',
+        'Zone Information,CORE_ZN,"Core, occupied"\n',
         encoding="utf-8",
     )
 
@@ -186,3 +186,17 @@ def test_parse_eio_preserves_category_columns_and_rows(tmp_path: Path) -> None:
         "Zone Name",
         "Description",
     ]
+
+
+def test_parse_eio_accepts_bracketed_data_category(tmp_path: Path) -> None:
+    """Bracketed category tokens remain supported for legacy EIO variants."""
+    path = tmp_path / "eplusout.eio"
+    path.write_text(
+        "! <Zone Information>,Zone Name\n"
+        "<Zone Information>,CORE_ZN\n",
+        encoding="utf-8",
+    )
+
+    tables = parse_eio(path)
+
+    assert tables["Zone Information"].rows == (("CORE_ZN",),)
