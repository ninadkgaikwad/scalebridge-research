diff --git a/src/scalebridge/integration/energyplus/__init__.py b/src/scalebridge/integration/energyplus/__init__.py
index 9a0787e..c458874 100644
--- a/src/scalebridge/integration/energyplus/__init__.py
+++ b/src/scalebridge/integration/energyplus/__init__.py
@@ -1,4 +1,13 @@
-"""Public EnergyPlus data-generation contracts."""
+"""ScaleBridge EnergyPlus integration.
+
+This package exposes the stable public contracts used to describe EnergyPlus
+simulation cases, identify scientifically equivalent cases across machines,
+record execution attempts, and serialize their manifests.
+
+Simulation execution and output parsing are implemented in internal
+subpackages. Callers should import shared contracts from this package instead
+of depending on internal module locations.
+"""
 
 from scalebridge.integration.energyplus.manifests.identifiers import build_case_id
 from scalebridge.integration.energyplus.manifests.models import (
diff --git a/src/scalebridge/integration/energyplus/manifests/__init__.py b/src/scalebridge/integration/energyplus/manifests/__init__.py
index c2b8738..14485fe 100644
--- a/src/scalebridge/integration/energyplus/manifests/__init__.py
+++ b/src/scalebridge/integration/energyplus/manifests/__init__.py
@@ -1,4 +1,9 @@
-"""EnergyPlus case and run manifest models."""
+"""EnergyPlus case identity and execution-manifest support.
+
+The manifest package separates immutable scientific case definitions from
+machine-specific execution records. It provides deterministic identifiers,
+validated Pydantic models, and atomic JSON serialization.
+"""
 
 from scalebridge.integration.energyplus.manifests.identifiers import build_case_id
 from scalebridge.integration.energyplus.manifests.models import (
diff --git a/src/scalebridge/integration/energyplus/manifests/identifiers.py b/src/scalebridge/integration/energyplus/manifests/identifiers.py
index d21e2cb..4bc2488 100644
--- a/src/scalebridge/integration/energyplus/manifests/identifiers.py
+++ b/src/scalebridge/integration/energyplus/manifests/identifiers.py
@@ -1,4 +1,10 @@
-"""Deterministic identifiers for EnergyPlus simulation cases."""
+"""Deterministic identifiers for EnergyPlus simulation cases.
+
+A case identifier represents scientific simulation intent rather than a
+particular execution. Machine names, absolute paths, campaigns, storage
+preferences, and tracking identifiers are therefore excluded. This allows the
+same case to retain its identity on Windows workstations and Linux HPC nodes.
+"""
 
 from __future__ import annotations
 
@@ -11,7 +17,25 @@ if TYPE_CHECKING:
 
 
 def canonical_case_payload(case_spec: CaseSpec) -> dict[str, Any]:
-    """Return the path- and machine-independent identity payload."""
+    """Return the canonical payload used to identify a simulation case.
+
+    Parameters
+    ----------
+    case_spec:
+        Validated scientific specification for one EnergyPlus case.
+
+    Returns
+    -------
+    dict[str, Any]
+        JSON-compatible content containing only identity-defining inputs.
+
+    Notes
+    -----
+    Output request order is normalized because requesting the same variables
+    in a different order does not change the intended simulation. Schedule
+    operation order is retained because later operations may depend on earlier
+    mutations.
+    """
     requests = sorted(
         (
             {
@@ -46,7 +70,12 @@ def canonical_case_payload(case_spec: CaseSpec) -> dict[str, Any]:
 
 
 def build_case_id(case_spec: CaseSpec) -> str:
-    """Build a stable case identifier from canonical scientific inputs."""
+    """Build a stable case identifier from canonical scientific inputs.
+
+    The identifier contains a readable ``epcase_`` prefix followed by the
+    first 24 hexadecimal characters of a SHA-256 digest. The complete
+    canonical payload remains available in the case manifest for provenance.
+    """
     encoded = json.dumps(
         canonical_case_payload(case_spec),
         sort_keys=True,
diff --git a/src/scalebridge/integration/energyplus/manifests/models.py b/src/scalebridge/integration/energyplus/manifests/models.py
index 23938f9..62e9333 100644
--- a/src/scalebridge/integration/energyplus/manifests/models.py
+++ b/src/scalebridge/integration/energyplus/manifests/models.py
@@ -1,4 +1,22 @@
-"""Pydantic contracts for EnergyPlus generation cases and execution records."""
+"""Validated contracts for EnergyPlus generation cases and execution records.
+
+The models in this module define the boundary between campaign orchestration,
+EnergyPlus execution, artifact storage, and downstream data processing.
+
+The central distinction is:
+
+``CaseSpec``
+    Immutable scientific intent shared across machines and execution systems.
+
+``RunManifest``
+    Persistent provenance for one execution attempt of a case.
+
+``GenerationResult``
+    Compact in-memory result returned to loops, workers, or schedulers.
+
+All models reject unknown fields and are immutable after validation. This
+prevents configuration drift after a deterministic case identifier is created.
+"""
 
 from __future__ import annotations
 
@@ -31,13 +49,13 @@ REPORTING_FREQUENCIES = {
 
 
 class FrozenModel(BaseModel):
-    """Immutable base model that rejects unknown manifest fields."""
+    """Base class for immutable, strict manifest models."""
 
     model_config = ConfigDict(extra="forbid", frozen=True, str_strip_whitespace=True)
 
 
 class RunPeriod(FrozenModel):
-    """EnergyPlus run-period boundaries."""
+    """Calendar boundaries applied to the EnergyPlus ``RunPeriod`` object."""
 
     start_month: int = Field(ge=1, le=12)
     start_day: int = Field(ge=1, le=31)
@@ -47,6 +65,7 @@ class RunPeriod(FrozenModel):
 
     @model_validator(mode="after")
     def validate_dates(self) -> RunPeriod:
+        """Validate real calendar dates and ordered dated-year periods."""
         validation_year = self.calendar_year or 2000
         start = date(validation_year, self.start_month, self.start_day)
         end = date(validation_year, self.end_month, self.end_day)
@@ -56,7 +75,7 @@ class RunPeriod(FrozenModel):
 
 
 class OutputVariableRequest(FrozenModel):
-    """One EnergyPlus Output:Variable request."""
+    """Declarative request for one EnergyPlus ``Output:Variable`` object."""
 
     variable_name: str = Field(min_length=1)
     key_value: str = Field(default="*", min_length=1)
@@ -67,6 +86,7 @@ class OutputVariableRequest(FrozenModel):
     @field_validator("reporting_frequency")
     @classmethod
     def normalize_reporting_frequency(cls, value: str) -> str:
+        """Normalize and validate an EnergyPlus reporting frequency."""
         normalized = value.casefold()
         if normalized not in REPORTING_FREQUENCIES:
             allowed = ", ".join(sorted(REPORTING_FREQUENCIES))
@@ -75,7 +95,7 @@ class OutputVariableRequest(FrozenModel):
 
 
 class ScheduleOperation(FrozenModel):
-    """A declarative mutation applied while preparing an IDF."""
+    """Declarative mutation applied to a schedule while preparing an IDF."""
 
     operation: Literal["replace_fields", "add", "delete", "rename"]
     object_type: str = Field(default="Schedule:Compact", min_length=1)
@@ -84,6 +104,7 @@ class ScheduleOperation(FrozenModel):
 
     @model_validator(mode="after")
     def validate_operation_fields(self) -> ScheduleOperation:
+        """Ensure each operation contains the fields required by its action."""
         if self.operation in {"replace_fields", "add", "rename"} and not self.fields:
             raise ValueError(f"{self.operation} requires at least one field")
         if self.operation == "delete" and self.fields:
@@ -94,7 +115,12 @@ class ScheduleOperation(FrozenModel):
 
 
 class CaseSpec(FrozenModel):
-    """Immutable scientific specification for one EnergyPlus case."""
+    """Immutable scientific specification for one EnergyPlus simulation case.
+
+    Input paths locate files on the current machine but do not define case
+    identity. Their SHA-256 values, simulation settings, output requests, and
+    schedule operations define the deterministic ``case_id``.
+    """
 
     schema_version: str = "0.1.0"
     case_name: str = Field(min_length=1)
@@ -124,17 +150,20 @@ class CaseSpec(FrozenModel):
     @field_validator("idf_sha256", "epw_sha256")
     @classmethod
     def normalize_sha256(cls, value: str) -> str:
+        """Normalize hexadecimal file digests to lowercase."""
         return value.casefold()
 
     @field_validator("timestep_minutes")
     @classmethod
     def validate_timestep(cls, value: int) -> int:
+        """Require a whole number of simulation timesteps per hour."""
         if 60 % value != 0:
             raise ValueError("timestep_minutes must divide evenly into 60")
         return value
 
     @model_validator(mode="after")
     def validate_unique_output_requests(self) -> CaseSpec:
+        """Reject duplicate variable, key, and frequency combinations."""
         identities = [
             (
                 request.variable_name.casefold(),
@@ -150,6 +179,7 @@ class CaseSpec(FrozenModel):
     @computed_field
     @property
     def case_id(self) -> str:
+        """Return the deterministic identifier for this scientific case."""
         from scalebridge.integration.energyplus.manifests.identifiers import build_case_id
 
         return build_case_id(self)
@@ -168,7 +198,7 @@ class RunStatus(str, Enum):
 
 
 class ArtifactRecord(FrozenModel):
-    """One artifact stored beneath a run directory."""
+    """Metadata for one artifact stored beneath a run directory."""
 
     role: str = Field(min_length=1)
     relative_path: Path
@@ -180,11 +210,13 @@ class ArtifactRecord(FrozenModel):
     @field_validator("sha256")
     @classmethod
     def normalize_optional_sha256(cls, value: str | None) -> str | None:
+        """Normalize an optional artifact digest to lowercase."""
         return value.casefold() if value is not None else None
 
     @field_validator("relative_path")
     @classmethod
     def require_relative_path(cls, value: Path) -> Path:
+        """Prevent artifact references from escaping the run directory."""
         raw = str(value)
         if value.is_absolute() or PureWindowsPath(raw).is_absolute() or raw.startswith(("/", "\\")):
             raise ValueError("artifact paths must be relative to the run directory")
@@ -194,7 +226,7 @@ class ArtifactRecord(FrozenModel):
 
 
 class ExecutionMetadata(FrozenModel):
-    """Machine and timing information for one execution attempt."""
+    """Machine, scheduler, and timing information for one execution attempt."""
 
     machine_id: str = Field(min_length=1)
     hostname: str = Field(min_length=1)
@@ -207,6 +239,7 @@ class ExecutionMetadata(FrozenModel):
 
     @model_validator(mode="after")
     def validate_timing(self) -> ExecutionMetadata:
+        """Reject execution completion times earlier than their start."""
         if self.completed_at is not None and self.completed_at < self.started_at:
             raise ValueError("completed_at must not precede started_at")
         return self
@@ -223,7 +256,7 @@ class SoftwareMetadata(FrozenModel):
 
 
 class ValidationSummary(FrozenModel):
-    """Post-run simulation and artifact validation summary."""
+    """Post-run EnergyPlus, signal, and timestep validation summary."""
 
     exit_code: int | None = None
     warnings: int = Field(default=0, ge=0)
@@ -236,7 +269,7 @@ class ValidationSummary(FrozenModel):
 
 
 class TrackingMetadata(FrozenModel):
-    """Optional external tracking references."""
+    """Optional MLflow references associated with an execution attempt."""
 
     mlflow_experiment: str | None = None
     mlflow_run_id: str | None = None
@@ -244,7 +277,7 @@ class TrackingMetadata(FrozenModel):
 
 
 class ErrorRecord(FrozenModel):
-    """Serializable failure details."""
+    """Serializable failure details for failed or invalid runs."""
 
     error_type: str
     message: str
@@ -252,7 +285,7 @@ class ErrorRecord(FrozenModel):
 
 
 class RunManifest(FrozenModel):
-    """Authoritative persisted record for one execution attempt."""
+    """Authoritative persisted provenance for one execution attempt."""
 
     schema_version: str = "0.1.0"
     case_id: str
@@ -269,6 +302,7 @@ class RunManifest(FrozenModel):
 
     @model_validator(mode="after")
     def validate_manifest_consistency(self) -> RunManifest:
+        """Validate case identity and status-dependent error information."""
         if self.case_id != self.case_spec.case_id:
             raise ValueError("case_id does not match the embedded case_spec")
 
@@ -281,7 +315,11 @@ class RunManifest(FrozenModel):
 
 
 class GenerationResult(FrozenModel):
-    """Compact in-memory result returned by a generation worker."""
+    """Compact in-memory outcome returned by a generation worker.
+
+    Batch orchestration can inspect this object without loading the complete
+    persisted manifest or any generated time-series artifacts.
+    """
 
     case_id: str
     run_id: str
@@ -307,6 +345,7 @@ class GenerationResult(FrozenModel):
 
     @model_validator(mode="after")
     def validate_result(self) -> GenerationResult:
+        """Validate timing and paired error fields."""
         if self.completed_at < self.started_at:
             raise ValueError("completed_at must not precede started_at")
         if (self.error_type is None) != (self.error_message is None):
@@ -315,5 +354,5 @@ class GenerationResult(FrozenModel):
 
 
 def utc_now() -> datetime:
-    """Return an aware UTC timestamp for manifest creation."""
+    """Return a timezone-aware UTC timestamp for manifest creation."""
     return datetime.now(timezone.utc)
diff --git a/src/scalebridge/integration/energyplus/manifests/serialization.py b/src/scalebridge/integration/energyplus/manifests/serialization.py
index 01db275..79695a8 100644
--- a/src/scalebridge/integration/energyplus/manifests/serialization.py
+++ b/src/scalebridge/integration/energyplus/manifests/serialization.py
@@ -1,4 +1,10 @@
-"""JSON serialization for EnergyPlus case and run manifests."""
+"""Atomic JSON persistence for EnergyPlus case and run manifests.
+
+Manifest files are first written beside their destination under a temporary
+name and then atomically replaced. Readers therefore see either the previous
+complete manifest or the new complete manifest, rather than partially written
+JSON after an interrupted process.
+"""
 
 from __future__ import annotations
 
@@ -10,6 +16,7 @@ from scalebridge.integration.energyplus.manifests.models import CaseSpec, RunMan
 
 
 def _atomic_write_json(model: CaseSpec | RunManifest, path: str | Path) -> Path:
+    """Serialize a supported manifest model with an atomic file replacement."""
     destination = Path(path)
     destination.parent.mkdir(parents=True, exist_ok=True)
     temporary = destination.with_name(f".{destination.name}.{uuid4().hex}.tmp")
@@ -32,12 +39,16 @@ def _atomic_write_json(model: CaseSpec | RunManifest, path: str | Path) -> Path:
 
 
 def write_case_spec(case_spec: CaseSpec, path: str | Path) -> Path:
-    """Write a case specification atomically as UTF-8 JSON."""
+    """Write a case specification atomically as UTF-8 JSON.
+
+    Computed fields such as ``case_id`` are omitted because they are derived
+    from the persisted scientific configuration during validation.
+    """
     return _atomic_write_json(case_spec, path)
 
 
 def load_case_spec(path: str | Path) -> CaseSpec:
-    """Load and validate a case specification."""
+    """Load and validate a case specification from UTF-8 JSON."""
     return CaseSpec.model_validate_json(Path(path).read_text(encoding="utf-8"))
 
 
@@ -47,5 +58,5 @@ def write_run_manifest(manifest: RunManifest, path: str | Path) -> Path:
 
 
 def load_run_manifest(path: str | Path) -> RunManifest:
-    """Load and validate a run manifest."""
+    """Load and validate a run manifest from UTF-8 JSON."""
     return RunManifest.model_validate_json(Path(path).read_text(encoding="utf-8"))
diff --git a/src/scalebridge/integration/energyplus/simulation/__init__.py b/src/scalebridge/integration/energyplus/simulation/__init__.py
index b700884..a9b0805 100644
--- a/src/scalebridge/integration/energyplus/simulation/__init__.py
+++ b/src/scalebridge/integration/energyplus/simulation/__init__.py
@@ -1,4 +1,9 @@
-"""EnergyPlus simulation execution contracts."""
+"""EnergyPlus simulation execution package.
+
+The package currently exports the worker-facing ``GenerationResult`` contract.
+Runner and generator implementations will be added behind this boundary in the
+next development block.
+"""
 
 from scalebridge.integration.energyplus.manifests.models import GenerationResult
 
diff --git a/tests/integration/energyplus/conftest.py b/tests/integration/energyplus/conftest.py
index bb76903..b98ee8c 100644
--- a/tests/integration/energyplus/conftest.py
+++ b/tests/integration/energyplus/conftest.py
@@ -1,3 +1,5 @@
+"""Shared test fixtures for EnergyPlus manifest contracts."""
+
 from __future__ import annotations
 
 import pytest
@@ -11,6 +13,7 @@ from scalebridge.integration.energyplus import (
 
 @pytest.fixture
 def case_spec() -> CaseSpec:
+    """Return a representative five-minute P1 EnergyPlus case."""
     return CaseSpec(
         case_name="small_office_seattle",
         building_type="OfficeSmall",
diff --git a/tests/integration/energyplus/test_identifiers.py b/tests/integration/energyplus/test_identifiers.py
index daad0a7..7bcb209 100644
--- a/tests/integration/energyplus/test_identifiers.py
+++ b/tests/integration/energyplus/test_identifiers.py
@@ -1,3 +1,5 @@
+"""Tests for deterministic, cross-machine EnergyPlus case identity."""
+
 from __future__ import annotations
 
 from pathlib import Path
@@ -6,6 +8,7 @@ from scalebridge.integration.energyplus import CaseSpec, OutputVariableRequest
 
 
 def test_case_id_ignores_paths_tags_and_compatibility_settings(case_spec: CaseSpec) -> None:
+    """Execution location and storage preferences must not alter case identity."""
     changed = case_spec.model_copy(
         update={
             "idf_path": Path(r"D:\different\model.idf"),
@@ -20,12 +23,14 @@ def test_case_id_ignores_paths_tags_and_compatibility_settings(case_spec: CaseSp
 
 
 def test_case_id_ignores_output_request_order(case_spec: CaseSpec) -> None:
+    """Equivalent output request sets must produce the same identifier."""
     changed = case_spec.model_copy(update={"output_variables": case_spec.output_variables[::-1]})
 
     assert changed.case_id == case_spec.case_id
 
 
 def test_case_id_ignores_output_request_annotations(case_spec: CaseSpec) -> None:
+    """Processing annotations must not change EnergyPlus simulation identity."""
     changed_requests = tuple(
         request.model_copy(
             update={
@@ -41,6 +46,7 @@ def test_case_id_ignores_output_request_annotations(case_spec: CaseSpec) -> None
 
 
 def test_case_id_changes_with_scientific_configuration(case_spec: CaseSpec) -> None:
+    """Adding an EnergyPlus output request must produce a different identifier."""
     changed = case_spec.model_copy(
         update={
             "output_variables": case_spec.output_variables
diff --git a/tests/integration/energyplus/test_manifest_serialization.py b/tests/integration/energyplus/test_manifest_serialization.py
index ee2d07e..ecb6a64 100644
--- a/tests/integration/energyplus/test_manifest_serialization.py
+++ b/tests/integration/energyplus/test_manifest_serialization.py
@@ -1,3 +1,5 @@
+"""Tests for lossless and validated manifest JSON persistence."""
+
 from __future__ import annotations
 
 from datetime import datetime, timezone
@@ -18,12 +20,14 @@ from scalebridge.integration.energyplus import (
 
 
 def test_case_spec_json_round_trip(tmp_path, case_spec: CaseSpec) -> None:
+    """A persisted case specification must survive a lossless round trip."""
     path = write_case_spec(case_spec, tmp_path / "case.json")
 
     assert load_case_spec(path) == case_spec
 
 
 def test_run_manifest_json_round_trip(tmp_path, case_spec: CaseSpec) -> None:
+    """A complete run manifest must survive a lossless round trip."""
     started_at = datetime(2026, 6, 23, 18, 0, tzinfo=timezone.utc)
     manifest = RunManifest(
         case_id=case_spec.case_id,
diff --git a/tests/integration/energyplus/test_manifest_validation.py b/tests/integration/energyplus/test_manifest_validation.py
index a9176fc..58ff3cd 100644
--- a/tests/integration/energyplus/test_manifest_validation.py
+++ b/tests/integration/energyplus/test_manifest_validation.py
@@ -1,3 +1,5 @@
+"""Tests for invalid EnergyPlus manifest states and unsafe artifact paths."""
+
 from __future__ import annotations
 
 from datetime import datetime, timezone
@@ -18,6 +20,7 @@ from scalebridge.integration.energyplus import (
 
 
 def test_timestep_must_divide_hour() -> None:
+    """EnergyPlus timestep minutes must represent a whole number per hour."""
     with pytest.raises(ValidationError, match="divide evenly into 60"):
         CaseSpec(
             case_name="invalid",
@@ -37,6 +40,7 @@ def test_timestep_must_divide_hour() -> None:
 
 
 def test_artifact_path_must_be_relative() -> None:
+    """POSIX absolute paths must not be persisted as portable artifact paths."""
     with pytest.raises(ValidationError, match="relative"):
         ArtifactRecord(
             role="raw_csv",
@@ -46,6 +50,7 @@ def test_artifact_path_must_be_relative() -> None:
 
 
 def test_windows_artifact_path_must_be_relative_on_all_platforms() -> None:
+    """Windows absolute paths must be rejected even on non-Windows systems."""
     with pytest.raises(ValidationError, match="relative"):
         ArtifactRecord(
             role="raw_csv",
@@ -55,6 +60,7 @@ def test_windows_artifact_path_must_be_relative_on_all_platforms() -> None:
 
 
 def test_failed_manifest_requires_error(case_spec: CaseSpec) -> None:
+    """Failed runs must preserve actionable error information."""
     started_at = datetime(2026, 6, 23, tzinfo=timezone.utc)
 
     with pytest.raises(ValidationError, match="require an error record"):
@@ -74,6 +80,7 @@ def test_failed_manifest_requires_error(case_spec: CaseSpec) -> None:
 
 
 def test_manifest_rejects_mismatched_case_id(case_spec: CaseSpec) -> None:
+    """A manifest must not claim an identity inconsistent with its CaseSpec."""
     started_at = datetime(2026, 6, 23, tzinfo=timezone.utc)
 
     with pytest.raises(ValidationError, match="does not match"):
