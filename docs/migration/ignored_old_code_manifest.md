# Ignored Old Code Manifest

The following sources should not be migrated into production modules:

- `Code/BuildingThermalModeling/`
- `BuildingModeling_BlackGreyBox_Condensed/`

Reason: older/bad code superseded by the current `Code/` workflows.

`BasicANN_Journal/` should be used only as a basic ML behavioral reference.
TensorFlow/Keras implementations should be rewritten in PyTorch.
